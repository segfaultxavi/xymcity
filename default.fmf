name "XYM City"

basegame xymcity

bind w +forward
bind s +back
bind a +moveleft
bind d +moveright
bind mouse2 +jump
bind mouse3 +forward
bind mwheelup impulse 10
bind mwheelup impulse 12

gamedir xymcity
game xymcity
alias f_startup "exec autoexec.cfg"
